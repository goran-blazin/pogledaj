import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'rs.pogledaj.app',
  appName: 'pogledaj',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
